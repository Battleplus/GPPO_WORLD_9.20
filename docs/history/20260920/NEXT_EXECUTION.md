# 下一轮可直接交给执行AI的指令

执行“紧急任务探针选择合同核对与单次闭环验收”。

## 1. 固定范围

冻结finite-communication-control-reserve-v1及当前通信实现。不训练、不更新模型、不修改正式规则或GPPO，不放宽mask、ACK、lease、fencing、能量、deadline或通信预算。

本轮只处理：紧急任务已于t=5合法获知，但探针在t=6选择NOOP的原因。

## 2. 先静态核对

读取最新emergency-admission-regression账本与探针源码。对t=5、t=6及之后已有记录列出：

- 紧急任务ID与当前槽位映射；
- 合法获知状态和各必要公开字段known/valid/age；
- 完整合法动作mask和对应任务的合法UAV–Task动作集合；
- UAV公开连接、能量、资源占用；
- 探针实际选择及命中的代码分支。

检查旧槽位/硬编码动作、任务身份映射、回退覆盖及是否实际无合法候选。不得从槽位可见推断命令合法；不得以隐藏task对象绕过公开信息。

## 3. 条件处理

若存在合法紧急候选但探针错误选择NOOP：只修独立合同测试控制器。公告合法送达前保持原公开规则；之后在当前mask允许的紧急候选中按已冻结公开排序和平局规则选择。不抢占、不取消已有任务，避免重复提交已有执行，所有命令续租走原链路。

增加纯函数测试：合法候选选择、可见但无候选、槽位变化、未获知不可提前选择、已有执行不被重复命令破坏。

若无合法候选：明确阻断字段/资源/合同条件，不修改mask、不伪造新鲜信息。现有证据足够时直接收口，不重跑。

## 4. 单次验证与硬预算

仅在上述探针错误修复且单元测试通过后运行同父场景I/event最多1episode。不要追加W1/W2。

沿用原SQLite、既有预算身份及稳定run_id。总上限256，最新报告verified204、unknown0、pending0；实际以回读为准，本轮最多剩余52步。真实环境fixture及失败全部计入。每次env.step前原子预留，异常unknown不自动退款。不能新建零消耗账本绕过旧预算。

CPU单进程4线程；optimizer0；墙钟上限30分钟。禁止循环重试。

## 5. 证据与结论

完整保存公告生成/准入/发送、合法获知、字段齐备、合法候选、选择、命令接受、ACK、续租、移动、物理到达和主机确认。记录时间、command_id、版本、lease、fencing、消息身份。物理到达与主机确认分别判断deadline。

下游失败时报告首个断点，不在同轮继续修改协议。成功只可表述“该固定I/event场景中，合同测试控制器选择合法紧急动作后的执行链通过”，不能称GPPO收益或总体弱通信可用。

输出NOOP原因、合法候选证据、最小补丁、测试、账本、预算快照、源码及制品哈希。保留旧失败和用户文件。本轮不自动commit/push/Release/转发。
