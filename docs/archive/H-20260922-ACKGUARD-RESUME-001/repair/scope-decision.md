# Guard 合同问题：待用户决定

handoff_id: H-20260922-ACKGUARD-RESUME-001

原授权禁止修改 guard。主代理在零环境步、零模型前向的纯函数核验中确认：当合法 action 0 概率为0.1、合法NOOP 24概率为0.9，且无continuation时，开启guard会选择0；关闭时选择24。该干预不仅排除已确认执行的任务，还强制优先分配。原20条记录没有发生NOOP覆盖，但不能由此保证后续分叉轨迹不会发生。

`proposed-guard-fix.patch` 只是未应用的具体建议：存在可选分配动作时也保留原mask合法的NOOP，再沿原概率排序。不调整概率、mask或任务排除集合。此文件不是已执行修复，也不授权继续矩阵。

如用户批准改变guard，需要另立执行单，记录新规则hash、旧两条是否仍在新规则下等价，以及能否在不重新执行旧分支的条件下合并。历史只有所选动作概率而非所有动作概率，不能凭其没有原NOOP就完整证明新规则选择在每一步一致；必须明确利用已保存原argmax、所选候选概率、NOOP合法性能够证明的范围，证据不足不得假设。

本单先完成runner修复及技术阻断审查，不继续22条、不增加预算或attempt。改规则、是否复用旧结果和续行均由下一张明确批准的执行单决定。
