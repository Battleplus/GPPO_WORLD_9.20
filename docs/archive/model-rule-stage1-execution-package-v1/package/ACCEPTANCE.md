# 阶段一执行包验收清单

状态：实现及集中验收中；正式执行未授权。

- [ ] 规则仅依据公开字段，固定优先级/截止时间/距离/能量/动作编号；同 guard、保留 NOOP。
- [ ] 合法 mask 与必要公开字段冲突时技术停止；未知且 masked 的原生字段可合法表示。
- [ ] 规则 rank carrier 满足共同接口，但不当作校准概率比较。
- [ ] 真正的 runner 入口在替身环境/模型下跑完 48 分支，并有故障留存测试。
- [ ] 规则分支不加载或调用模型，不推进 learned hidden；模型分支按实际选择推进。
- [ ] 每次调用在执行前后分别计数，失败成本不伪装为成功或退款。
- [ ] 首对比较相同初始环境和公开观测，不要求两个策略选择相同动作。
- [ ] runner 输出由正式 analyzer 接收并复算；缺失/损坏关键证据拒绝输出主效应。
- [ ] 原生奖励、终止、cohort、parent bootstrap、delta=0.01 及预算明细一致。
- [ ] 授权器仅在明确批准后更改原数据库上限 1067→1625，历史 857 步保留。
- [ ] 源码及传递依赖身份冻结；实际命令、pending 模板和全包哈希就绪。
- [ ] 零正式环境步、零模型前向、零 snapshot/checkpoint 加载、零 attempt、原 SQLite 未变。
- [ ] 独立归档分支提交与远端回读核验完成。

总体研究目标仍 active；第一阶段包通过不代表算法验收通过。
