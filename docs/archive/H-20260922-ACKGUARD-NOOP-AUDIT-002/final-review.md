# 主代理最终审查：NOOP 合同修正与历史复用

handoff_id: H-20260922-ACKGUARD-NOOP-AUDIT-002

状态：CLOSED。本单修复、审查与证据交付完成；动态实验未获授权且未运行。

执行：luna_worker / gpt-5.6-luna / max；主代理等待执行者完成后集中核验。只修改guard候选排序、纯函数测试及本单审查脚本。runner哈希保持不变。合法NOOP进入剩余候选，原概率、较小动作索引tie-break、任务身份排除和continuation处理保持不变。

独立回归35 passed，覆盖无排除、NOOP最高、最高分配被排除后选NOOP、平局、仅NOOP、continuation跨UAV同任务排除及非法动作不恢复。原18passed/15passed1failed记录保留；其唯一失败是旧测试断言原错误行为，现已明确更正为新合同，未掩盖历史失败。

|分支|A：保存观测下可证动作不变|B：概率证据不足|C：已证不一致|完整复用判定|
|---|---:|---:|---:|---|
|parent-00 repeat-0|6|4|0|证据不足，不作为新规则轨迹复用|
|parent-00 repeat-1|6|4|0|证据不足，不作为新规则轨迹复用|

每分支step 1/4/5/7属于B；两条首步即为B，后续A只证明给定保存观测及原argmax记录下的单次选择，不证明新轨迹会到达相同状态。未保存完整候选概率；无独立历史基础快照结束后hidden/cache比较，状态为not_recorded。不能用现在重新载入补足。C=0也不等于全轨迹等价。因此不合并新旧规则作完整矩阵效用分析，原两条保留为旧规则历史制品。

核验：本单12项制品哈希、原21项历史制品哈希、15项源码/输入身份全部匹配。SQLite只读integrity=ok，384上限、reserved=verified=20、unknown=pending=0、历史attempt总数1；文件SHA保持700b0c9173c8debd5cacb8066328dd2359bb913a128f3018710c8129de5e6223。本单环境步、模型前向、全部更新、新attempt均0。剩余22条及旧2条均未执行。

新guard SHA-256：2bc314ff9e3d4f78ec871c1a2e739a92669b1f9bd573aabce6a8be5b3e9e808a。

下一阶段建议：封存旧两条，不按“补22条”拼成新规则矩阵。若未来另行授权动态评价，使用修正guard、明确登记完整矩阵和预算归属，并保存完整候选概率及每分支前后基础快照摘要。已有20步不退款；本单没有授权新执行或预算。

本单归档范围仅文档、diff、测试和证据索引，不含模型或原始大型制品。远端提交及精确回读记录另存final-remote-verification.json。
