# 执行包勘误 v2

前一归档 dd2a542fad99a14225ec3faf7416bdb038d85c34 的24个文件确已回读一致，但这只证明上传身份，不证明运行器正确。前次“执行包完成”“首对检查已修复”“额外两次读出已登记有效”的表述撤回；当时30项测试未覆盖完整执行入口，未提供足够证明。

独立静态审查发现：首对函数覆盖同臂逐步记录而比较末步；执行入口未启用额外读出，局部计数硬编码1；授权模板runner哈希过期；分析器manifest路径未绑定。随后读代码还发现probe输出引用未定义probabilities。均属运行包技术缺陷，不是算法结果。

修复采用固定首对唯一step1、其余分叉记录不强配对；启用并记录两个额外同输入读出、复用原encode/world；修复输出与H005 digest绑定；分析器核对额外读出数值、counter、输入、manifest及首步门。新增完整入口stub测试，所有替身调用都不是真实env.step/模型前向。

此前和本次均未执行动态实验、加载checkpoint或快照、修改正式SQLite。正式账本仍299/404。旧包本地保留于 research-reviews/world-event-feature-package-006/previous-package-dd2a542，旧远端提交不改写。动态申请撤回后，以修正包重新申请一次授权。
