# H-20260923-GPPO-WORLD-MAINLINE-006：阶段一执行包

状态：准备执行申请，未获动态授权。研究目标保持完整；本包完成不等于主线目标完成。

## 研究决策与证据复用

复用 H-005 最终结果与 H-006 集中预检，不重新执行历史矩阵、不重新推理或恢复快照。
H-006 归档提交为 `9f192b0818c032de54194ccf424f6694d6309a1e`，判定 A 限定为事件候选特征关闭敏感性。
原生 M10 25-action 的世界模型17维候选特征进入 actor logits；其中末5维事件概率存在既有 `use_events=False` 关闭接口。guard 后279决策中NOOP-only94、单分配7、多分配178。

H-005 的基线是冻结策略加公开执行状态 guard；并非已证明一个不含模型的规则控制器。此处两臂保持同一冻结策略、世界模型和guard，仅改变指定actor输入。任务目标中的整个世界模型增量仍不能由这一个局部干预直接证明。

## 唯一实验与解释范围

| 问题 | 证据 | 允许结论 | 禁止结论 |
|---|---|---|---|
| 该冻结actor是否依赖事件候选输入，且影响本开发矩阵效用 | 同输入特征/分量logits核验，以及normal减event_features_off的父级配对效用 | 指定关闭干预下的依赖与敏感性 | 世界模型全部净收益、训练作用、偏好学习、未见泛化 |

关掉事件输入是已有API约定；零不代表校准的“无事件”预测，未证明该checkpoint训练时见过这种输入。分布外风险必须和任何结果同时报告。保留其余12维、raw world输出、selected by_action hidden和policy hidden正常推进。动作发生分叉后，后续状态可合法分叉。

## 固定矩阵和统计

使用已观察train开发父场景parent-00..07，W1，seed1101，prefix0，每父场景3个原外生repeat，两臂各24条，全部新执行。每对同一外生键，每臂独立恢复副本；未将H005轨迹直接当新臂对照。完整48行顺序见paired-manifest.json；按parent/repeat排序，臂先后按(parent index+repeat)奇偶平衡。

主偏好(0.8,0.2)，任务/能耗尺度(0.5,1.0)，gamma0.99。效用为首个恢复后步骤k=0起的 `sum(0.99**k*(0.4*r_task+0.2*r_energy))`。原生终止，不添加终端价值。每父场景先平均3个配对差，再8父场景等权；10000次父级bootstrap，random.Random(20260923)，线性分位点0.025/0.975。绝对差<=1e-6仅为描述性平局，推断保持未舍入值。

次指标：原登记task_ids cohort和全部任务分开报告；物理到达/主机确认、任务完成、能耗、接受/拒绝/task_unavailable、guard变化、合法NOOP、模型调用和实际计时。未执行动作的接受/结果不从隐藏真值推断。相同输入的排序比较仅限明确同输入门，不逐时点强配对分叉后的轨迹。

## 执行顺序和预算提案

1. 零步执行包检查、纯函数/stub测试、冻结所有身份和命令。
2. 用户一次性批准本包的动态范围及同库预算变更后，才允许扩额、登记run/attempt、加载模型和恢复环境。
3. 原SQLite299/404；未来总上限提案1067=299既有+768新阶段，增加663；新run cap768。现有105步不是新阶段授权。本包不写正式SQLite、不新建替代预算。
4. 完成第一对的两臂接口门，每臂与其自身初态/合同核对；之后一次执行余下固定矩阵，不逐分支请示，不重复运行失败样本。
5. 原生horizon18、prefix时间2、间隔1，每条最多16步；到cap仍未原生终止则技术停止，不截断补标签。
6. 分析只读已保存记录；逐reservation/branch/step关联finalization与SQLite再判verified。写入时pending不等同于最终未核销。

最多768次环境步、768次policy encode、768次world候选batch（每batch25行）、770次actor readout（含首对两次额外同输入读出），全部参数更新0。分量logits复用实际forward输出，不额外调用模型获取日志。记录setup/model/probe墙钟时间及分支总时间；分支时间减probe得到环境与runner合计开销（含guard、预算、奖励复算和日志），不当作独立env.step延迟；调用减少不能替代总计算成本结论。运行时长尚未实测，不编造GPU小时。

## 判定、停止与后续

CI下界>0：支持正常事件输入在指定干预下占优；上界<0：支持其退化；覆盖0：不确定。技术不完整一律不提供主效应，不当作算法负结果。不改主偏好、不删父场景、不补样追正。

身份、非法动作、恢复隔离、hidden推进、随机性、门控、必要标签或预算异常立即停止，保留实际消耗；不自动退款或重试。纯解析错误可在原始证据充分时零步修正，保留失败分析和勘误。

本阶段完成后再决定保留事件输入、封存此干预或提出一个具体缺证问题。无论结果正负，均不能直接进入偏好训练、事件触发成本实验或泛化测试；这些仍需阶段决策和单独预算授权。已封存R/K与事件同配方负结果不重启。
