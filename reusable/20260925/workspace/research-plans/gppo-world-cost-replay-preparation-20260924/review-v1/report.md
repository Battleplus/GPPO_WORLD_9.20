# 固定轨迹成本诊断：当前冻结配置超过成本门槛，暂停正式大矩阵

本轮依据用户在明确成本诊断预算之后回复“继续吧”执行一次。授权只覆盖0环境步、三类模型前向各525次、651次规则决策、更新0、最多600秒。本轮完成并停止，无重试或预算追加。

## 研究决定

模型在独立进程、整批CPU计时下，后20遍平均为 **13.625ms/决策**，高于冻结的10ms采用门槛。包括首次调用的全部21遍平均也为 **13.779762ms**。这是当前硬件、Python/PyTorch版本、CPU intra4/inter1、冻结控制器及保存技术门输入下的成本不达标证据。

按预先规则，**暂停当前冻结配置的172800步正式收益比较，不进入世界模型归因**。不改10ms阈值、不调线程、不改模型或规则、不追加重复来追求通过。没有独立父场景正式收益结果，因此不能宣称GPPO任务效果无效或总体研究目标已完成；也不能推荐优先部署当前模型。

## 完成量与身份

- M：25条保存决策×21遍，encode/world/actor各525次，三类合计1575次。
- R：31条保存决策×21遍，规则决策651次，不加载Torch。
- 环境构造/reset/step均0，新环境episode为0；optimizer/world/offline更新均0。
- M/R各一个独立子进程，M正常退出后再启动R；均退出码0。每臂完整21遍全部纳入，没有按速度筛掉重复。
- 动作复现：M525/525、R651/651；M选择后的world hidden摘要525/525一致。模型权重前后检查通过；源码、配置、checkpoint和输入文件身份复核一致。
- 新阶段SQLite reserved=verified；unknown=0、pending=0、integrity_check=ok，唯一attempt。环境步计数0/0。历史账本和技术门余额未消费。

## 实测成本

| 口径 | M：GPPO＋world | R：公开历史规划器 |
|---|---:|---:|
| 首遍CPU ms/决策 | 16.875000 | 11.592742 |
| 后20遍CPU ms/决策 | 13.625000 | 11.970766 |
| 全部21遍CPU ms/决策 | 13.779762 | 11.952765 |
| 后20遍各批CPU均值范围 ms | 10.000000–17.500000 | 11.088710–13.104839 |
| 后20遍逐决策wall p95 ms | 4.759345 | 38.419305 |
| 全部完整子进程CPU s | 11.531250 | 7.937500 |
| 完整子进程wall s | 6.123499 | 8.274858 |
| 峰值RSS MiB | 220.203125 | 27.832031 |

完整进程CPU由父进程在子进程退出后读取，包括启动、加载、输入读取、校验和输出；与计时批次控制器CPU分开。批次内没有SQLite、日志持久化或hidden哈希操作。批次包含公共输入复制、记忆/过滤、模型或规则计算、最终动作及hidden推进。CPU为进程线程累计时间，不能用wall更短来抵销CPU超限。

旧技术门33.75ms与本轮13.625ms来自不同测量边界，不能宣称优化降耗；本轮未优化算法。新口径减少了逐步量化与同进程线程池污染，超限信号仍在。两臂重放各自真实轨迹，输入分布不同，不能把比值当相同工作负载的普遍性能比。R本身约11.971ms；若部署也要求R满足10ms，它也不合格，不能因比模型便宜就直接推荐部署。

## 证据边界

所有回放来自已有gate-000/001，不新增独立父场景。21遍只是计时重复，不是新增任务收益样本；不计算任务效用置信区间或新完成率。M后20遍的各批均值最小10ms、最大17.5ms，聚合均值13.625ms；没有因个别批次恰到10ms而挑选通过结论。

wall p95和内存处于原包络内，但不替代CPU门。当前诊断不评估未来硬件、不同线程配置、量化/编译优化或其它任务分布；不能将本次成本结论外推到所有GPPO/world实现。没有环境执行，因此不能补齐旧运行的环境与通信CPU，也不能声称完整现场系统成本已验证。

研究阶段状态：阶段1问题与比较协议已准备；阶段2功能门完成，但当前配置未满足成本门，独立父场景任务增量仍未检验；阶段3–5未启动。后续若要重新开放当前方案，须提出明确的新方案及其独立预算，保留本次负结果，不把调整阈值/参数后的结果拼回冻结验证。

## 制品与保留

原始运行：../runs/cost-diagnostic-v1；analysis.json含全部计数与成本统计；input-hashes.json固定原始运行身份；hashes.json固定本审查包。授权、worker计数、各批原始时间、完整进程CPU、SQLite均保留。复核仅用保存文件和只读SQLite，无新模型前向或环境步。

未commit/push/Release，未连接服务器，未向其他任务发送消息。
