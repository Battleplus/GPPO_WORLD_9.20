# 最小配对诊断：A 成本门停止

结论：暂停本次attempt。数值与历史重放检查通过，但移除组N平均控制CPU为 **10.386905 ms > 10 ms**，触发冻结的绝对成本门。未执行B/C，800个新配对episode均未开始。不能判定收益保持、直接移除的任务损失，或世界模型独立训练贡献。

## 本次事实

- 3个worker各完成21批×25个决策；reference与candidate每批逐位一致，full历史动作/guard/world hidden复现。
- N wall p95=3.534460 ms，低于50ms限额。
- 全21批N/full CPU比=0.79318182，描述性节省20.6818%。相对节省没有抵消绝对CPU门失败。
- 后20批比=0.80582524，仅如实报告；不替换冻结的全21批主口径，不删冷启动、不重跑以跨过边界。
- 不能由本次跨时间计时差异反推机器负载、线程调度或计时器是唯一原因；未额外开展计时实验。

## 消耗与边界

新增env.step=0、reset=0；encode/actor各1575；world525；加载3次；规则决策与全部更新0。
SQLite reserved=verified，unknown=pending=0，integrity=ok。旧账本哈希未变。
记录到终止状态的完整父进程+worker CPU=32.734375s，wall=19.182571s，保守合计RSS峰值=721293312bytes。3个worker均exit0且权重不变；主runner因预设成本门exit1。状态写入后的父进程尾部不冒充精确测量，另保留wall30s/CPU600s上界；报告复核开销独立记录。

阶段2加本attempt累计env.step=133662，encode/actor各65626，world=64576，加载=9。旧阶段结论与全部历史消耗保留，不退款、不转阶段额度。

## 研究决策

阶段2组合系统优于强规则的正式结论不变。本次简化检验止于绝对运行成本门，任务收益保留证据仍缺失。归类为技术/成本停止，不能当作算法负收益，也不能支持删除或保留world的独立贡献论断。暂停，不自动启动B/C、重试、调阈值、训练或追加样本。本次冻结申请结束；未使用额度不是新尝试授权。
