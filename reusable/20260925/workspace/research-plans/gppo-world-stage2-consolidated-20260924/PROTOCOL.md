# 阶段2合并协议：冻结策略的必要计算路径与实际增量

状态：用户已批准本包 A→B→C 条件执行及原分项额度；执行前完成零步 RSS 口径统一并重新冻结身份。本包是下一项阶段2工作的唯一决策包，旧决策/实验包作为证据保留。

## 目标与已有结论

唯一目标仍是在独立父场景上判断冻结GPPO＋世界模型是否值得优先于合理公开历史规则。当前原实现因保存技术输入上的13.625ms CPU/决策超过10ms门槛而暂停；不能撤销这一结论或回填优化后的成本。

新工作只提出一个实现候选：不计算决策消费者不用的world next_state头、policy vector critic及其它unused输出切片；保留全部模型权重、encoder、GRU、17维候选特征、所有候选hidden、mask、偏好、公共过滤、规则和线程配置。不是训练、剪枝权重、压缩模型、移除世界模型或组件归因。

2026-09-24回读远端指定路线分支head仍为7395b3e985a94a12156d5ea5f54d6c47d1536c72。AGENTS、ROADMAP、RESEARCH_STATE、NEXT_EXECUTION、EVIDENCE_AND_MIGRATION已核对。仓库资料早于本地技术门与成本结果；最新用户自主执行约定和最新本地结果优先，旧运行建议不自动复活。

源码证据：joint_gppo.py的predict_all_candidates已经一次forward处理25候选。forward同时计算next_state，但actor的17维输入仅来自policy_feature8、vector_reward2、task_consequence2和sigmoid(event_logits)5；在线world hidden取最终action对应latent。evaluate_encoded的vector_value不进入logits/probabilities，也不推进hidden。候选源码decision_only_inference.py只复用这些必要模块，未改原生源码。

这仅证明依赖关系上存在可省计算，未证明数值等价、节省量或能达到10ms。需要至少约26.61%的当前CPU均值下降才能从13.625ms降到10ms，候选没有这种保证。只尝试该候选一次，不搜索更多实现直到通过。

## 研究问题、身份和公平比较保持不变

原问题：延迟公开观测下，当前UAV—任务分配怎样影响后续任务的截止窗口、能量与资源释放。模型可用公开历史和动作条件一步特征；R是利用同样公开历史、ACK与自己未确认提交的确定性滚动规划器。强规则是否仍有可学习的不足仍是假设。

原生历史M10 25-action、W1、arrival_to_region/physical_arrival/single_shot；不混入当前有限通信修复版或D-02。继承gppo-world-decision-freeze-20260924/protocol.md第1–5、7–8节全部环境、模型、奖励、控制器与统计合同。P_train checkpoint SHA-256 bf10d2685a4a3e9da036689f5028b022330e86e922e09c95a7dd0a929df9bb1a，偏好(.8,.2)，CPU intra4/inter1，全部更新0。candidate为额外执行路径，不覆盖原权重或方法。

## 一次授权覆盖的条件执行链

实现与常规纯函数测试由执行AI自行完成。动态工作开始前把实际统一runner、候选、基线、分析器和输入哈希写入单一执行manifest；不得看到动态结果后改实现、输入、门槛、重复数或基线。当前candidate及统一端到端runner已装配；不得将静态准备称为动态验收通过。

装配更新：统一入口run_pipeline.py、stage_worker.py、runtime_support.py及analyze_formal.py现已准备，只有纯函数/桩对象检查，尚未实际运行原生worker。执行manifest绑定实际代码后再受预算授权控制。

为避免规则进程仅因包级re-export就加载Torch，两臂统一采用透明包路径shim，跳过仅含re-export与__all__的原生__init__.py，实际原生子模块文件不变并逐个验哈希。初始化文件若出现其它可执行逻辑即停止。奖励复算使用原joint_training.py中原样AST提取的_vector_reward函数及NumPy，避免R导入训练/模型模块；提取函数身份绑定完整原文件。R须确认未加载Torch。此导入方式写入运行证据，不冒称执行了原包初始化。

两持久worker按顺序执行；父调度器只对自己创建的worker使用Windows进程暂停/恢复，使闲置模型线程池不与规则计算并行竞争CPU。完整生命周期CPU与wall仍保留。暂停/恢复或计时API失败即技术停止，不静默换实现。此隔离机制尚未进行动态worker验证，由获批链条的接口门承担。

### A：行为等价与成本（零环境步）

只用已经观察的gate-000/001的全部25条M公开决策，不读取正式场景或heldout。冻结原实现与candidate各一个独立进程，每个完整回放21遍；每个parent从空hidden/公开记忆开始，顺序和线程固定。参考实现先、候选后。首次、后20遍与全部21遍均报告。

参考进程在首遍批次结束后保存每个决策的编码、policy hidden、全部25候选17维特征、全部候选world hidden、logits和概率作为等价输入；从计时内保留原张量引用，不在计时内做IO/哈希/额外前向。候选在同一公开轨迹上自主计算，计时后核对上述结果。

要求：所有有限张量bitwise相等，全部21遍的最终动作与旧轨迹完全相同，选中hidden摘要一致。离散字段/输入mask/公开过滤完全一致、权重不变。若浮点差异，即使动作没变也停止；不事后放宽容差。该门只覆盖所测输入，不声称全状态空间形式证明；还须由源码依赖审查支撑精简。

计时沿用已验证的独立进程+整批process_time口径，记录逐决策wall、完整子进程CPU、启动/加载、峰值RSS。只有候选后20遍CPU/决策≤10ms、wall p95≤50ms、RSS≤4GiB，且全部一致性通过，才进入B。参考是否变快不改变候选10ms门槛。保留冷启动和全部21遍结果，不挑最快重复。

A不重复R计算；其源码和已有独立成本证据保持不变。计时重复不增加父场景样本量，不计算新任务收益。

### B：正常环境接口验证

A通过后自动执行，不再单独索要同一实验内部批准。使用原生生成器2个另列技术parent、各candidate-M/R一次，horizon≤18，最多72步。为避免重复旧环境轨迹，本包固定新的gate父seed为924110000、924110001；正常reset，W1 mixed，exogenous_key采用本包唯一ID加parent/repeat。只按公开既有身份元数据去重；冲突即停止，不换seed。

复用原信息权限、共同过滤、规则、奖励与终止检查。记录candidate实际代码身份，不能把其字节身份称为原实现。真实模型hidden按最终动作推进，所有标签可独立复算。技术门不要求模型取胜，不以观察到的任务结果选场景或决定样本量。

与旧runner相比，必须记录启动到退出的完整进程CPU、总wall、模型/规则控制计算、环境、账本/日志分项及未归类开销；使用互斥时间区间避免重复计费，报告线程池活动造成的归属限制，完整进程成本绝不扣除或隐藏。两臂使用独立持久worker进程，R不加载Torch；正式阶段复用同一进程结构。记录每worker的完整CPU与RSS，禁止把M进程内线程池CPU归给R。分项CPU仍有量化时保留聚合口径，不将0解释为无开销。

B通过接口、信息/身份、全成本记录与记账门后自动进入C；若成本记录仍不足以支持10ms判断，停止，不在正式运行中临时换口径。技术parent不纳入正式估计。

### C：独立父场景的唯一主比较

沿用原已规划但从未运行的1600个parent、3个外生重复、M/R两臂，最多9600 episodes、172800步。seed 924200000..924201599，原生成器train枚举/mixed/W1；元数据角色为independent_development_eval。固定顺序与交替两臂次序、相同parent/repeat随机键。两持久进程按调度顺序串行执行，不并行竞争CPU，不额外预热。

正式场景全部纳入，不按候选空间、失败、模型胜负或时延筛选。只能在看到任何模型结果之前生成并冻结完整tape身份；与允许使用的历史身份索引去重。原生done停止，缺失不填0，不人为延长到主机确认。执行后原生终止前的已观察确认与最终投递结果明确区分。

原生主效用U=Σ .99^k(.4*r_task+.2*r_energy)，r_task=float32((new_completed-new_expired)/6)，r_energy=float32(-max(0,Eprev-Enow)/36)。parent内3次差值取均值，1600个parent等权。10000次parent bootstrap、random.Random(20260924)、95% percentile CI。delta=0.01原生效用单位，不改成百分点。

采用门：CI下界>0.01、按时物理到达及原生终止前已观察按时主机确认的parent宏平均均不下降，M控制器平均CPU≤10ms、wall p95≤50ms、RSS≤4GiB；同时报告总进程成本、能耗、完成/过期、分项及通信发送/接收/丢失。不把研发包络称现场经济盈亏线。规则成本照实报告，不宣称等计算预算或已符合部署要求。

正式过程中不查看聚合效应来调样本数；技术异常与资源上限仍立即停止。正式统计完成后：实用门通过→准备阶段3归因方案；上界≤0.01或任务/成本门失败→简化/暂停该candidate；否则不确定并停止。此授权不覆盖阶段3–5实验或训练。

## 全部资源上限（一次集中授权，不可相互借用）

| 子阶段 | env.step | encode/world/actor各 | 规则决策 | wall上限 | 制品上限 |
|---|---:|---:|---:|---:|---:|
| A 原实现与候选等价/成本 | 0 | 1050 | 0 | 20分钟 | 1GiB |
| B 正常episode技术门 | 72 | 36 | 36 | 1小时 | 1GiB |
| C 正式主比较 | 172800 | 86400 | 86400 | 36小时 | 40GiB |
| 全部条件合计 | **172872** | **87486（每类）** | **86436** | **37小时20分钟** | **42GiB** |

三类前向合计262458。GPU=0；所有阶段optimizer/world/offline更新=0；最多同时一个工作worker执行，父调度器及全部同时驻留worker合计RSS上限4GiB，暂停的worker也必须计入（不能用多个进程各4GiB绕过），全部日志和分析制品计入42GiB。进程完整总CPU按实际记录，不等同wall。

完整进程CPU另设A≤1 CPU小时、B≤4 CPU小时、C≤144 CPU小时，条件总上限149 CPU小时；计父调度器及所有子进程，不把单次controller净CPU当全部消耗。到任一CPU或wall上限均停止。磁盘记录采用固定gzip level=1保存完整逐步JSON内容，不能按结果删字段；B必须验证压缩后可逐条恢复原记录及标签。压缩/日志CPU纳入完整进程成本，不能混入模型收益或扣除掉不报告。

环境正常reset上限：B4次、C9600次，总9604次。A不构造环境。模型加载上限A2次、B1次、C1次，共4次；不可通过重载/多worker偷偷加前向。没有额外模型、线程、偏好或world开关搜索；不计入任何免费的冒烟/预热。

原SQLite余额不属于本授权。若批准，在一个新阶段账本内给A/B/C分别设定上限与唯一子阶段键，全部纳入同一授权总量。子阶段早停剩余不得挪给下一阶段或重试。批量诊断需先整臂预留，完成后核验计数再verified；失败unknown不退款。环境每步执行前预留，标签及持久化确认后verified。完整矩阵无法完成则不冒称通过。

## 失败处理与恢复规则

任何实测数值/动作/hidden不一致、身份/模块/信息边界异常、非法动作、标签缺失、配对或账本不一致、unknown/pending、计数超限、资源超限、非原生截断均立即停止相关执行，并禁止后续子阶段。

本包没有自动重试额度：动态开始后的实现缺陷，保留失败与消耗，定位原因和准备修复可以零步进行；若要再跑，须新范围/预算授权。动态开始之前的纯函数/静态修复由执行AI自行完成，不逐项请求确认。不得以新建SQLite、删失败或隐瞒尝试恢复。

授权A+B+C后，满足明确条件的内部推进不再询问用户；只有超范围/预算、数据边界、目标变更或外部发布才请求决定。不commit/push/Release，不向其它任务发送消息。

## 获批后的零步 RSS 统一

2026-09-24 用户明确批准本包原有 A→B→C 分项及总额度。所有阶段的4GiB门均指父调度器与全部同时驻留worker合计，包含暂停worker。Monitor.check 不按 suspended 状态过滤进程，只排除已经退出的worker；使用父进程及每个仍驻留worker的各自 PeakWorkingSetSize 之和进行保守门控，避免把非同时出现的峰值之和误称实测瞬时RSS峰值。该上界可能提前停止，但绝不放宽4GiB门。单worker峰值仅为诊断项，不能单独代表资源门通过。监控在等待响应时最多约0.2秒一次及命令边界执行，属于软件监控，不宣称操作系统硬内存配额。零步桩测试验证暂停worker仍计入、父子总和超限即停止。授权证据与更新后的协议、执行清单哈希绑定后才开始唯一一次尝试。
