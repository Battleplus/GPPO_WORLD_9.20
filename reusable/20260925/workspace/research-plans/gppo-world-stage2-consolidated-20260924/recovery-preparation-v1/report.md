# 原固定矩阵零步恢复准备结果

**4034个已完成episode全部可保留，剩余5566个。任务语义与原固定矩阵统计合同可保持；恢复必须明确作为分段续行，不能冒充从未中断的成本测量。本轮已完成准备并停止，未获准启动恢复。**

## 保全结果

- 4033项有原始episode汇总，1项完整执行但原汇总缺失；没有已开始却尚未结束的episode。
- 缺失项：eval-0672/repeat-0/R，12步全部存在。逐步vector reward按原float32公式逐步复算、折扣效用、终止标签、能耗、任务与确认字段与原terminal payload一致；初始公开哈希来自已保存reset记录。仅在新derived-summary-provenance.json生成派生汇总，附来源gzip哈希和解压行号，原episodes.jsonl未追加。
- 已完成4034项恰为原固定顺序前缀，覆盖2017个完整M/R配对。reset公开哈希、外生键、tape身份全部对应，无重复无缺项。
- 下一项为eval-0672/repeat-1/R，随后同pair M。剩余两臂各2783项，逐项清单已冻结。不会以4033条原汇总误判重跑最后12步。

## 恢复有效性与比较边界

Worker每episode独立创建环境和PublicMemory，显式state=None重置policy/world hidden；奖励边界和计数重置。PublicPlanner不保留跨episode计划状态；原通信随机结果由seed/link/语义key哈希确定，不依赖跨episode全局RNG进度。模型使用eval/no_grad及确定性argmax，无可影响下一episode的动态训练统计。

stage_worker.py、decision_only_inference.py、analyze_formal.py与实际原执行文件字节一致。场景复用原1600项清单，不调用生成器；环境/模型/checkpoint/控制器/奖励/偏好/统计方法及0.01门槛不变。源码依据和静态/桩测试见continuation-boundary.md。这是可恢复性的源码与已存证据支持，不宣称本轮做过动态模型等价重验。原A/B通过证据复用，不申请或执行整套重验。

恢复完成后原4034加新5566一起评价；不计算部分矩阵主效应或模型输赢。旧与新全部C决策时延原样合并，包括各段首次，不选较好部分；新启动、加载、恢复校验、压缩日志、统计和收尾开销计入并单列段别。

旧资源快照在最终status写入和退出之前。已测旧C wall=9554.4737019s、CPU=8622.4375s；已测A+B+C wall=9572.7176203s、CPU=8649.34375s。旧尾部无法精确回补，申请固定占用30s wall/600s CPU作为预算保守预留，而非实测或已证实上界。恢复在新上限内部预留同样的收尾空间。严格的“旧进程精确全生命周期CPU”仍存在历史计量缺口；不能据此虚构完整成本。该限制不改变原控制器10ms/50ms和4GiB合计RSS标准。

## 观察方式修复

观察器使用Windows CreateFileW，明确FILE_SHARE_READ|FILE_SHARE_WRITE|FILE_SHARE_DELETE，context manager保证句柄清理；异常观察只返回诊断，不重启或干预实验。只读独立observer-progress.jsonl，不再读正在写入的正式episodes/steps/SQLite。

实际发布器采用独立进度文件追加，发布失败不传播到实验数据路径。临时文件测试覆盖并发反复append/fsync、同时读、半条JSON、多个读者、读取异常、观察进程异常退出和句柄释放；实际恢复调度器的进度发布函数在读取器持有文件时80次追加均成功。

**23 tests passed**；未启动真实worker、未导入模型、未调用原生环境。恢复循环用纯桩验证只经过5566个原定剩余键。未授权入口在创建worker/账本前拒绝执行。

零步准备中的两个实现问题已修正并保留记录：派生汇总最初把info额外rejected计数误纳入task_counts，对齐原verify_vector只保留completed/expired后完全一致；独立进度的原子替换方案在本机临时文件测试中失败，因此最终改用已测安全的独立追加方案。失败测试输出保留，没有触及实际实验或旧记录。

## 一次性恢复资源申请（当前未批准）

| 项目 | 原C上限 | 已用C | 名义余额 | 本次申请 | 恢复后C最大累计 |
|---|---:|---:|---:|---:|---:|
| 环境步 | 172800 | 56381 | 116419 | 100188 | 156569 |
| encode各类前向（world/actor同值） | 86400 | 26575 | 59825 | 50094 | 76669 |
| 规则决策 | 86400 | 29806 | 56594 | 50094 | 79900 |
| reset | 9600 | 4034 | 5566 | 5566 | 9600 |
| 模型加载 | 1 | 1 | 0 | **额外1** | **2** |

原A+B+C模型加载总额4已用完；申请明确增加1次，总额变为5。其余不借用A/B余额。100188=5566×18，由horizon18、dt1、每episode正常reset及原生终止合同核实；不是新增场景或扩大矩阵。

新段wall上限**33小时（118800秒）**，完整进程CPU **141小时（507600秒）**，制品 **38GiB**，父调度器与全部同时驻留/暂停worker合计RSS **4GiB**，GPU/全部更新 **0**。加上历史已测C与旧尾部预留后，wall至多128384.4737019秒、CPU至多516822.4375秒，仍在原C的129600/518400秒内；原数据、报告和新制品累计也低于原C保守40GiB包络及总42GiB包络。前述至多数值是授权会计口径，不把未测历史尾部变成实测事实。

含旧A/B的整个条件实验，新旧环境步最大156621，encode/world/actor各最大77741，规则最大79930，reset最大9604，模型加载5，训练0。实际完成时按旧实际加新实际报告，不把申请上限当实际消耗。

新账本只作为不可退款的追加分段账本，保存旧SQLite路径、SHA256和所有历史stage消耗，最终生成累计表；旧SQLite不写入。原失败不清零，未知/未完成预留不退款。

## 停止条件与交付

身份或合同不符、原顺序/配对改变、标签缺失、非法动作、非原生截断、预算不明或资源门失败立即停止并保留；没有第二次自动重试、补样、调参、训练或发布。恢复次数仅一次，源身份/授权校验先于新账本和worker创建。

入口：run_recovery.py；协议：RECOVERY_PROTOCOL.md；一次申请：RECOVERY_BUDGET_REQUEST.json；绑定：recovery-manifest.json；差异：source-diff.patch及identity-differences.json。授权模板approved=false，没有authorization-approved.json、新实验SQLite或attempt。91项执行/输入身份已绑定。

本轮env.step=0，模型前向=0，optimizer/world/offline update=0，新增环境attempt=0。已完成/缺汇总/未执行键清单均在本包，旧源/原始日志/SQLite哈希保持。当前仅等待对本恢复包及其明确成本边界的一次性授权；准备本身不是恢复许可。
