# 本地第二次恢复准备完成，尚未启动

可保留 8637/9600 个完整 episode（89.96875%），剩余 963 个：一个已执行 7 步的中断项和 962 个未开始项。当前剩余的是阶段2冻结模型评估，不是训练，也不代表全部研究阶段即将结束。

## 已完成

- 原矩阵的精确唯一键后缀已固定，M 481 / R 482；不重跑 A/B 或完整项。
- 中断项 eval-1439 / repeat-1 / R 的 reset 与 7 步原记录已提取，并用既有 CRC/未压缩 digest 核验来源。正式恢复需逐步精确重建，通过后续行；本轮未实际重建。
- 已实现单连接预算缓解、完整 SQLite 错误诊断、续行与授权门；原 SQL、模型执行器、推理、公开规则与分析器不变。
- 14 项零环境步测试通过，包含完整 963 键的桩调度、前缀差异停止、预算等价、异常退出持久性、资源上限和授权门。测试不是环境级验证。
- 历史正式数据库保持原哈希，旧资源不退款；本轮没有创建正式运行目录或 attempt。

## 必须保留的不确定性

SQLite 历史 disk I/O error 的底层原因未查明。单连接 WAL/FULL 与诊断是明确标注的缓解，不是已证明根因修复。恢复必须获得用户对该前提变更的明确接受；不能仅用测试通过声称长期可靠。

两次停止造成的恢复开销全部保留。没有已验证的对象快照，因此重建中断项需额外 1 次 reset；总加载还需新增 1 次。原 7 步与重放 7 步都计实际消耗，但只保留一次统计和原 timing，不选择更快测量。

## 一次性申请

| 资源 | 本次上限 |
|---|---:|
| 环境步（含 7 步重建） | 17334 |
| reset | 963 |
| encode/world/actor，各 | 8658 |
| 规则决策 | 8676 |
| 新增模型加载 | 1 |
| wall / 完整进程 CPU | 8小时 / 32 CPU小时 |
| 全部驻留进程合计 RSS | 4 GiB |
| 新增制品 | 8 GiB |
| 训练更新 / GPU | 0 / 0 |

额外授权边界：C reset 累计 9600→9601；模型加载累计 5→6（C 2→3）；明确接受未确定历史 I/O 根因下的已测试缓解。8小时是硬上限，不是预计耗时。

旧 A/B/C 环境步为 120263，本次上界后累计 137597；C 为 120211→最多137545，仍低于原172800。其他累计预算及关机预留详见 RECOVERY_BUDGET_REQUEST.json 和 RECOVERY_PROTOCOL.md。本次不借用其他阶段额度。

## 有效性与去留

静态身份与调度测试支持保持原实验；真实续行有效性仍以获批后的 7 步精确重建和所有技术门为条件。前缀不一致或任何异常立即停止，不自动重试。

本轮未查看部分矩阵输赢，没有实用增量结论。完整矩阵后按冻结阈值、父场景统计和成本护栏决定：准备世界模型归因协议、简化/暂停，或不确定并指出缺口。阶段3—5、训练与 Git 发布仍未授权。

本轮 env.step=0、模型前向=0、全部更新=0、新增正式 attempt=0。只运行临时文件和标准库桩测试；历史制品未修改。

申请 attempt：consolidated-v1-recovery-2-local-once。入口 run_recovery.py --authorization <用户批准后形成的绑定授权文件>；模板默认 approved=false，直接运行不能启动实验。
