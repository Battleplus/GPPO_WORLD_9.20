# 条件实验结果：技术中止，暂停

## 实用增量

尚未建立。A逐位等价/成本门及B正常reset技术门通过，C因 episode 汇总文件写入 PermissionError 停止。C逐步记录包含4034个原生done，episode汇总仅4033条，未达到9600条固定矩阵；没有用部分样本计算主效应、置信区间或胜负结论，也未事后补写原始汇总。这是技术失败，不是算法负结果。

## 成本

A候选热态CPU 8.9375ms/决策，wall p95 2.960375ms，达到10ms/50ms门；本次参考为9.90625ms，旧13.625ms诊断保留，不能将跨次成本差异全归因于实现简化。

已执行C部分的模型CPU均值9.394991ms、wall p95 6.255460ms；规则CPU均值16.086317ms、wall p95 61.999525ms。这里只描述固定运行的已执行部分，不能当作完整正式矩阵成本验收或相同轨迹速度比。

调度器终态记录：wall 9572.718s（约2小时39分33秒），父及worker完整CPU 8649.344s（约2.40 CPU小时），其中父进程6144.688s；保守RSS峰值和336.344MiB，包含父调度器及全部驻留worker、暂停worker不排除。软件轮询及各进程峰值上界不等于内核硬配额。终态记录之前约1.107GiB制品，最终制品字节数见decision.json，均低于原分项/总上限。外部监控命令和最终只读审查不计入控制器推理时间；完整进程CPU数值是冻结runner父子进程边界，不声称涵盖IDE或所有外部审查进程。

## 消耗与核验

- 环境步56433，reserved=verified，unknown=0，pending=0；A0、B52、C56381。
- encode/world/actor各27647；规则决策29836；显式reset4038；模型加载4；所有训练更新0。
- 两份gzip完整读到EOF并通过CRC；逐步行数、两臂调用数与SQLite verified账本一致；SQLite integrity=ok；66项绑定身份全部一致。
- 最后一条为eval-0672/repeat-0/R/step-11，done=true。它的12步已完整持久化且verified，汇总写入失败，因此不改为unknown，也不隐瞒汇总缺失。
- 所有原运行worker已退出。没有重跑、补样、换候选、调参、训练、阶段3—5实验或Git发布。

## 停止原因与责任

执行AI的进度监控采用System.IO.File.ReadLines，它在Windows上的共享方式会阻止并发Python追加写入。零环境步临时文件复现：读取器持有文件时追加报PermissionError errno13，释放后成功。此机制与本次episodes.jsonl写入错误直接吻合；没有故障瞬间内核句柄追踪，不能声称排除了全部其它持有者。这是本次监控实现缺陷，责任不归于模型、环境任务或用户。

原运行源码、协议、SQLite与失败记录保持。此次授权禁止自动重试，故不因为缺陷在监控端而自行恢复或挪用余额。

## 决定

暂停当前执行；不进入组件归因，也没有证据据此选择算法简化。若今后考虑恢复，唯一前置工作是零环境步修复和验证不干扰写入的观察方式，再按明确恢复范围处理；本报告不提出新的自动实验。

适用身份为历史原生M10/W1、一个冻结P_train模型seed；没有当前有限通信环境收益、偏好/事件收益、训练seed泛化或部署结论。
