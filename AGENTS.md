# 交接工作约定

- 用户让主助手负责研究决策、方针和给执行AI的指令；除明确要求实际执行外，不把规划自动变成训练任务。
- 每次收到实验报告，先用中文说明证据支持什么、尚未支持什么，再给出完整可复制执行指令。避免只有“建议继续”而无边界。
- 用户已取消自动转发，不向其他Codex任务发送消息。
- 核心目标是GPPO＋世界模型＋事件预测/事件触发＋偏好学习。规则是对照，失败假设可以关闭，不能擅自放弃整个研究目标。
- 真实有限通信包括公告、遥测、命令、ACK、续租和完成通知。actor调用减少不等于通信节省或完整计算节省。
- 世界模型不能伪造新鲜遥测、ACK、版本、lease、fencing或合法执行权限。
- 不用训练掩盖合同实现错误；不反复筛选失败样本制造模型机会。新实验需冻结问题、对照、数据划分、预算和停止条件。
- 冻结test/heldout一经用于开发判断，不再作为后续独立盲测。历史validation结果不能冒充独立收益。
- 环境步、策略更新、world更新、离线监督更新分别记账；失败尝试也计资源消耗。SQLite预留/确认是预算依据，unknown不退款。
- commit指针是有效恢复依据，不按最大checkpoint文件名恢复；禁止拼接未提交尾部。
- 物理到达与主机确认分开；EXPIRED是明确失败，不是删失；证据缺失不能当零。
- 父场景bootstrap不覆盖训练seed不确定性。记录内零安全违规不代表生产安全通过。
- 保留历史负结果、失败记录、旧Release和用户staged/unstaged/untracked文件，不reset、clean或覆盖。
- Git签名曾缺失私钥。不得擅自关闭签名或改全局配置；远端失败时如实说明，不无限重试。
- 不上传凭据。当前服务器不是下一步的必要条件，不自动连接或安装依赖。
- 新仓库是后续规划归档位置；源码迁移必须显式列清单、核验哈希，不能把仅有文档称为完整部署。
